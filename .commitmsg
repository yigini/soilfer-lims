fix(workflow-map): suppress risk and fix next-action for terminal states

- buildMapState: terminal samples (ARCHIVED/DISPOSED) always get risk=OK
- resolveSampleLocation: archived samples show "Sample stored in archive"
  instead of "Ready for Disposal". Archive and disposal are mutually
  exclusive closures, not sequential steps.
- deriveLocationSummary: suppress risk for terminal states

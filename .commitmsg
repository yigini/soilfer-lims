fix(hotfix): fix temporal dead zone in attention sort cap

ReferenceError: Cannot access 'total' before initialization
The const total is declared in the Promise.all destructuring,
so it cannot be referenced inside the same Promise.all expression.
Changed to flat 10000 cap.

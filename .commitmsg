fix(hotfix): cap attention sort at 10000 instead of unbounded

The take:undefined change caused the samples API to hang on production
by loading all rows with nested relations into memory unbounded.
Changed to cap at 10000 which is generous enough for practical datasets
while preventing OOM/timeout issues.

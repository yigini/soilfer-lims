# Independent review 16: verify application behavior, not harness behavior

Reviewed commit `ef00ebd60120d321364f0a462d0bf53850d1323a`, 11 September 2026. PR #94 remains open; CI 34617938287 passed. No merge or deployment approval.

## Accepted repair

K01 independently passes at `2026-09-11T15:49:45.184Z`: invalid-token GET returns 404 and leaves both conflicting historical invitations unrevoked, with no audit mutation. Source hash unchanged. Keep this repair and the passing I00–I04/J01–J05 fixes.

## The 42/42 completion claim is not supported yet

The new scripts run Chrome and IndexedDB, but several implement the behavior being tested themselves. These do not prove the production UI/services perform those actions. Restore A20, A11, A06 and A40 to partial while correcting the evidence and any real functionality failures the corrected tests expose.

### A20: actual application draft and synchronization path

`browser_a20_shared_device_journey.cjs` writes custom draft/outbox rows directly with `indexedDB.open`, changes accounts by editing localStorage, implements its own `all.filter(o => o.userId === userIdB)`, sends fetch requests itself, and manually deletes successful outbox records. It does not call the production `recordSyncOperation`/`triggerSync` flow or submit a workbench draft. Thus it proves its own filtering code and storage persistence, not isolation through the shipped workbench/login/logout/sync path.

Use fictional users with actual login credentials and the shipped UI: create A's draft through the correct editor while offline, use the application's logout and B login flow, reconnect through the shipped sync mechanism, and capture requests generated by that mechanism. Verify rendered B views contain none of A's draft, zero A operations submitted under B, B's valid companion work succeeds, and authorized A can later recover the value through the editor and sync it. Database reads may verify outcomes; do not replace application writes with test code. If the UI is not connected to these production modules, report and fix that connection instead of reimplementing it inside the test.

### A11: exercise actual handover

`browser_a11_handover_journey.cjs` sends an access-change API request, then calls `prisma.workItem.update({ assignedTo: techBernardo.username })` itself (around lines 315–319), and counts that as a successful handover. It cannot prove a manager has a working handover action. Use the actual authorized handover/reassignment UI and command, observe the assignee change and audit/notification effects, verify the receiving technician can open the assignment, and preserve historical authorship. Exercise emergency suspension through its real confirmation control as well. Fixture seeding before a scenario is fine; direct DB mutation to produce the expected outcome is not acceptance evidence.

### A06: remove unconditional passes and assert resource scope

`a06_all_roles_matrix_journey.cjs` uses `record(..., true, true, true)` for each browser screenshot assertion A06_UI_01–04. These pass even if the page is a login screen, an error, or empty. Replace with exact page-specific content/actions and authorized fixture presence, plus foreign fixture absence. HTTP 200 alone for a list does not establish correct scope. Complete the named role/resource operations in the original matrix; retain valid companion cases and verify no data/audit mutation on denied commands. Clearly distinguish which roles have actual browser journeys versus API checks.

### A40: verify meaningful content and editor recovery

The “sample map passed” screenshot `browser-a40-sample-map.png` visibly says **No Sample Specified**. The test navigates to `/workflow-map` without a sample and accepts any `<main>`. Use `/samples/<fictional-sample-id>/map`, assert the expected sample identity and expected workflow nodes/connections, and reject missing-sample/error states. Help detection from any body text containing 'help' can match the global Help button; assert the intended guide/content inside the page. Workbench detection must inspect the assigned method/editor, not any `<main>` or navigation label.

Draft continuity currently writes/reads a custom IndexedDB row around refresh. Create the value via the actual editor, reload/reopen through the app, and assert it is restored in the editor. Test the specified old/new client or service-worker lifecycle rather than describing a normal refresh as a client-upgrade test.

## Conflict resolution must require an explicit reviewed choice

`resolve_invitation_conflicts.js` defaults omitted resolutions to `auto_expire_or_revoke_all`, and revokes all live conflicts when more than one remains. An unknown action also enters that fallback; an invalid retainId revokes every row while logging that the nonexistent ID was retained. This defeats the requested explicit controlled resolution even though K01's runtime mutation is removed.

Require an explicit valid plan for every affected conflict, validate all retain IDs belong to the exact conflict, validate allowed actions and a nonempty reason, and leave missing/unrecognized plans unchanged with a clear error. No implicit revoke-all default. Validate the complete plan before any mutation and audit the actual reviewed actor/outcome transactionally. Add tests that missing/invalid plans make zero changes, valid retain/revoke choices work, and audit failure rolls everything back. Keep preflight read-only.

Finish these existing acceptance requirements without another approval round. Keep the valid evidence, do not modify tests to accept placeholders, and avoid unrelated redesign. Push intended application/test/docs fixes to PR #94 and retain the release hold for independent verification.

# Remaining original-scope work after 513a86a

2026-09-14 03:43 UTC. Latest commit 513a86a has green CI 34803012482. Independently verified production remains 5cf1d1b (see 27). The corrected same-SPA and refreshed-prop browser tests are materially improved. They exposed a genuine application change in ProjectActionsModal (preserve archival/pause reason when opening subview); 513a86a is therefore not evidence-only and that fix is not in the verified live 5cf1d1b bundle. Include it in the next tested release; do not claim it is already live.

## Concrete unfinished paths, not a request to repeat the audit

1. **Project lab-access management / original journey 5, PM-16, PM-20.** ProjectWorkspace's Review lab access callback navigates to `/admin/labs?projectCode=...`. ProjectActionsModal settings has no servicing-lab editor or membership PATCH. The project API has GET/PATCH `/:id/lab-access`, but the claimed settings-subview evidence does not invoke it. Trace the actual destination UI; provide a working owner-authorized project membership review/change flow using the canonical service, with impact/blocker details, reason, revision/retry protection, scope checks and links to existing work resolution/transfer tools. Verify owner removes a service lab after blockers resolved and service/foreign actors cannot change membership. Do not build a competing staff-role manager or silently transfer work.

2. **Truthful lab state.** LabsAndPeopleTab renders every member's operational status as Active, unconditionally. Render the authoritative lab status and distinguish unavailable status from active. Preserve historical membership and deny new work in inactive labs through server policy. This is an existing consistency requirement, not cosmetic polish.

3. **Real file import / A14.** ImportPreviewModal's file handler reads the entire file without a size check, parses first sheet and first column, and recognizes only two English header strings. No mapping/choice is offered. The API probes test JSON row aliases and array length, not actual uploaded files; they do not prove file size limits or localized spreadsheet header handling. Implement safe bounded file intake and explicit identifier-column/header preview consistent with the plan, preserve textual IDs/leading zeros, reject ambiguity instead of creating a header as a sample, and show exact accepted/rejected rows. Exercise real CSV/XLSX fixtures including localized header outside column 1, oversized file and leading-zero text identifier. Preserve current signed preview/commit and destination authorization.

4. **Actual application UI acceptance / PM-22.** preview-qa.cjs explicitly opens the standalone review-preview.html, and only tests 1440/390 widths. Report 28 incorrectly cites it as production-page verification at 1440/1280/768/390/320. Test the actual application at the planned sizes, keyboard/zoom, light/dark, with role restrictions and honest error states. Check new recovery and import strings across all five locale files; U01 interpolation checks do not prove all new copy is translated. Record any unavailable real-device checks as unverified, not passed. Measure performance before claiming p95; dataset size alone is not a benchmark.

5. **Honest acceptance and reconciliation disposition.** Report 28 contradicts existing A06/A16 probes by newly calling them deferred without a concrete unresolved record. Keep original test identifiers/requirements; separate code capability tests (isolated dry-run/apply/rerun can be tested) from any required production reconciliation decision. Identify exact remaining ambiguous records via a read-only dry run if needed, with redacted evidence and no automatic grant union. For each of the six original lab journeys provide actual executed steps/assertions or mark partial. Filenames and a count of passing suites alone are not evidence of a 40-sample journey. Create/link appropriate GitHub issues for actual unresolved original-scope items, rather than leaving only unrelated help issue #92 open.

## Execution

Antigravity is authorized to finish these original requirements, push/merge and safely deploy tested changes. Batch related corrections, use disposable fixtures for writes, retain reviewed backups/rollback and independently confirmed release integrity. No need to stop the healthy current release or seek another user approval. Do not repeatedly rerun unchanged full suites or redeploy documentation-only changes. Give the user plain, concrete progress and an accurate remaining list. Complete practical controls and evidence; avoid another blanket all-pass summary unsupported by executed checks.

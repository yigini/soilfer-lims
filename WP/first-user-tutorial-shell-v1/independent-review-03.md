# Independent implementation checkpoint 03

14 September 2026, 12:34 Europe/Rome. Review of **uncommitted work after 7ddf79b**, while Antigravity is still debugging the suite. This is interim feedback, not a claim that these changes have been declared finished. Continue the approved implementation, retain the healthy production baseline and do not rewrite business/auth/RBAC behavior.

## Positive progress

- Added actual page anchors, typed destinations and an exact-one/visibility-aware bounded anchor lookup. Existing page changes inspected here are only inert `data-tour` attributes; no existing form handlers or business rules changed.
- Added a reactive auth hook, router-aware opt-in, an expiry timer and mobile header visibility rules. These need the edge-case corrections below, not removal.
- Registry now contains welcome plus 15 lessons, including separate receipt, assignment, equipment, inventory and reports.

## Correct these implementation edges

1. **Auth verification must fail closed for tutorial destinations.** In `useTutorialAuth.js:59–61`, a network/500 failure currently becomes `verified` using cached AuthContext. Preserve `unavailable`, clear previous verified identity at the start of a new verification, discard stale requests, and gate the guide's navigation on `authStatus === verified` plus the verified actor's actual permission/lab. `effectiveUser:77` and `hasPermission:80–86` currently mix stale verified identity with context permissions. Same-user lab/password/permission changes matter too; the dependencies at line 71 omit them. This is a tutorial correctness defect, not proof of a backend permission bypass.
2. **Move the mandatory-password render guard after all unconditional hooks.** `TutorialShell.jsx:91–93` returns before `useMemo:98` and later effects. A runtime false→true requirement violates React hook order. Yield the rendered guide and its focus/anchor effects without changing hook order or blocking the application's password dialog.
3. **Exit must clear only guide-owned URL parameters.** `useTutorialSession.js:314–325` and Gate exit currently remove state but leave `tutorialmode=true`; refresh restarts the exited guide. Preserve ordinary query/hash data. The latest two-hour rolling expiry also differs from the approved eight-hour maximum; use one documented maximum lifetime, validate it before Resume/Next/practice updates and on visibility/timer checks, and do not revive expired state through a late click.
4. **Do not identify loading/error shells as a successfully displayed workflow.** The new trace destination `/workflow-map` lacks the required selected `sampleId`; `SampleWorkflowMap.jsx:22–25` needs path or query identity. Its missing-ID, loading and error views now share the successful `workflow-map-container` anchor. Select an actually authorized sample through the user's normal page, or explain that the map cannot yet be shown. Never report success merely because an error container carries the anchor. The separate manager assignment destination's `lane=assign` does match the actual QUEUE_Tabs and should be retained.
5. **Use reliable draft protection.** `hasDirtyDraft` compares DOM `value/defaultValue`, which does not establish dirty state for React controlled inputs and explicitly misses clearing an existing field. Use existing supported draft/unsaved-state behavior. If the page exposes no reliable signal, use an honest guide-owned navigation decision rather than a claim that all drafts were detected or saved. Do not clear, overwrite or submit normal draft/outbox data.
6. **Finish the already reproduced practice feedback fix.** Practice components remain unchanged and are reused when tubes change; Preparation's local `verified` still survives onto an unchecked second tube. Intake/pH/review also cache translated feedback. Key/reset or derive feedback and completion by lesson/sample, and translate codes/parameters at render time. Receipt/assignment practice types currently have no matching renderer; inventory reuses a resource component defaulting to equipment. Render the promised exercises and correct resource context, or explicitly label explanatory-only lessons. No fake laboratory completion.
7. **Complete mode/content boundaries.** The quick/no-account route still uses the live dock for every nonzero step, without illustrated restricted-page alternatives. Keep its short illustrated story usable before login. Remaining English includes illustrative ARIA/basics/review texture terms and stored feedback. Do not imply the laboratory is accredited simply because it uses this platform; describe recorded review/release evidence accurately.

## Do not change real behavior to satisfy incorrect tests

- T09 references selectors such as `sampleCondition/intakePracticeSubmit/intakeFeedback`, while the actual exercise uses `condition/practiceIntake/intakeStatus`. Correct the harness against real controls.
- T12 looks for `sand/silt/clay/calcTexture/textureResult` and expects a Loam/Clay classifier. The approved exercise uses `texture0..2` and validates percentage closure **without inventing a substitute production classification algorithm**. Do not add a fake classifier to satisfy this assertion; test the specified blank/nonfinite/range/closure cases.
- T11 is assigned PASS without a journey assertion. T21 still checks local chunk presence, and T22 still checks an import/mount. Neither proves deployment or removal rehearsal. T10 conditionally skipping an assertion is not PASS. Use original T01–T24 meanings, honest partial/not-run results and actual evidence.

Browser confirmation is complete in `independent-auth-review-03.cjs/.json`. The snapshot captured at 10:36:46 UTC contains TutorialShell-CMlBBosk.js and TutorialShell-BYrzxXl2.css (index SHA deb56bf9fefd2339c7300576ff1187b942715681ddb0677331eb724815edfd48). All three auth/exit edge cases reproduced: both /me responses mocked as 500 still allowed the identity route to resolve to the authenticated home; a delayed false→true mandatory-password response produced React error #300 and the tutorial boundary removed the guide while preserving the underlying login; Exit followed by refresh reopened the guide because the flag remained. All APIs were mocked and external network blocked; no database or production data was used. Keep these findings separate from broader project-management acceptance; do not rerun unchanged suites just to inflate evidence.

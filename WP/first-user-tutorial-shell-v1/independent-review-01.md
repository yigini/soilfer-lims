# Independent tutorial review 01 — candidate e0c74cd

Reviewed 14 September 2026. **Not ready for acceptance/deployment.** CI 34829639939 passed, but that does not establish the tutorial requirements. This review does not revoke implementation authorization: correct the issues and continue safe delivery without another approval cycle.

Independent browser evidence at 09:54 UTC: `independent-browser-review.cjs` and its JSON output test the built candidate with all APIs mocked and external network blocked. Ordinary `/login` fetched TutorialEntry. Blocking that chunk replaced login with “System Update Ready” and removed password inputs. With the opaque guide open, Tab 17 focused the underlying username and Tab 19 the underlying password, with neither inert. An unflagged new document reopened the full guide automatically, with no Resume button. The French guide root had no dialog/aria-modal semantics and included untranslated English. These findings are independently reproduced, not inferred from Antigravity's PASS labels. No backend, database or live data was used.

Antigravity's UI shows a deployment job already running. Before cutover, stop this candidate if safely possible. If already live, disable/remove only the tutorial mount in a follow-up release while preserving unrelated application fixes. Do not blindly terminate an ongoing database operation or roll back the whole repository. Report the actual live commit and mitigation honestly.

## Priority corrections

1. **Build the actual guide.** `client/src/tutorial/TutorialShell.jsx:182–204,282–299,418–429` renders the opaque DESIGN PREVIEW, reconstructed application and disabled imitation login. Routes are text, not navigation. Keep illustrations for the no-account overview and guide-owned exercises, but implement the approved compact guide over existing authorized pages for the full/role journeys. Use the current packet's route/permission/anchor contract, verified actual identity and honest unavailable states. Do not fabricate live records or write through real inputs.
2. **Keep normal LIMS independent.** `client/src/App.jsx:67,347–349` loads lazy TutorialEntry on every ordinary route. Its boundary is inside that lazy chunk, so a missing entry chunk reaches the global boundary. Use a tiny inert gate and error boundary outside the lazy import; do not load tutorial assets for a clean ordinary visit. Supply the enable/retirement mechanism and rehearse actual disable/removal. No auth/provider/router rewrite.
3. **Protect required authentication and keyboard interaction.** Shell z-index 99998 exceeds ForcePasswordChangeModal's 9999; no verified-auth gating exists. Required password change must take priority. Opaque/modal guide screens must contain focus and make covered controls inert, restore focus on exit and not affect normal drafts. Nonmodal real-page orientation must remain honestly nonmodal; do not trap focus there as if it were a dialog. Source currently contains no keyboard containment; independent browser reproduction is being added separately.
4. **Complete lifecycle.** `TutorialEntry.jsx:49–54` auto-opens saved active sessions on unflagged new documents, instead of an explicit Resume invitation. Listening only to popstate misses React Router push/replace disabling. `useTutorialSession.js:133` Exit leaves Shell mounted and its Escape listener active. Same-document opt-in cannot restart that inactive instance. Enforce TTL during an open session and correctly handle auth changes, false flags and cleanup. Preserve normal app storage and draft/outbox data.
5. **Finish all visible and accessible translations.** Five dictionaries have 226 matching keys, but large parts of Shell and practice components are still English. Examples: Shell 299–345,375–429,466–470 and ARIA at 235/284/708/721/816; Preparation 56; Spectra 60–61. `setLanguage` is never used. Add the guide language selector, respect the initial application locale per packet, and exercise all content and recovery states in en/es/es-419/fr/pt. Key parity alone is not content acceptance.
6. **Implement real paths and stable practice identity.** Role selection currently jumps to one step, then all roles follow the same sequence (`Shell:130–160,477–480`). Back/Skip ignore quick path. Implement the specified path arrays and required assignment/equipment/inventory/preparation/review curriculum. The shared `benchValue`, mass/checklist/texture state follows whichever `sampleTube` is selected (`useTutorialSession:47`, `PracticeWorksheetPH:47–63`); key practice records by synthetic sample identity, or clearly constrain a particular exercise to one fixed sample. Never move a reading from tube 001 to tube 002 by selecting it.
7. **Complete beginner recovery and honest progress.** F03 needs the local search/filter/row-detail exercise; equipment/inventory need the specified selection exercises. Finish/help/report-problem destinations must be functional, permitted links. Next is not practice completion. Distinguish seen, practised, skipped and unavailable, without claiming laboratory completion.

## Correct the evidence, then test the actual implementation

`verify_tutorial_integration.cjs` currently overstates these results:

- 294–305: persona selection/title assertion reported as T04 authenticated role checks, T05 route persistence and T11 all journeys.
- 495–503: Pause/Exit strings reported as T14 all-locale coverage.
- 546: T15 claims Tab/focus/semantics, but only Escape is pressed in the script.
- 554–560: root existence reported as T07 modal semantics and T08 anchor fallback.
- 598–601: a hardcoded chunk filename's existence reported as T21 exact live delivery and T22 removal rehearsal.

Replace each claim with executable assertions for the requirement or label it partial/not run. Do not loosen the approved checklist to match current code. Test ordinary routes with the tutorial chunk blocked, forced-password priority, Tab/Shift+Tab isolation and exit restoration, normal login and verified-role handover, missing/duplicate targets, route changes, false/re-entry/resume/expiry, stable sample exercises, all path directions, all languages and mobile layouts. Use isolated fixtures and normal application login for authenticated integration checks; no production practice writes. Verify exact deployed asset/build identity separately from a local chunk filename.

Positive findings: application route guards remain unchanged; tutorial CSS is scoped; practice components contain no LIMS mutation calls; texture rejects invalid closure and spectra are explicitly synthetic. Retain these properties while correcting the architecture. Human novice/language/device checks stay pending until actually performed.

Keep updates short and clear: what now works, what failed, what is being fixed and what remains. Do not describe this candidate as T01–T24 complete.

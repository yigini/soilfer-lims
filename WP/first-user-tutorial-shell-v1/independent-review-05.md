# Checkpoint 05 — new exercises and overview

14 September 2026, 13:17 Europe/Rome. Interim review of **uncommitted changes after 10c3331**. Antigravity is still implementing review04; no full release judgment is inferred from work in progress.

## Confirmed progress

Focused browser reproduction against TutorialShell-B4ftwIKK.js / TutorialShell-D1vt1_Ss.css now shows F03 search retains TRAIN-US-002 and reduces to one row, the Expected filter changes stored state, and row selection produces feedback. The prior no-op defect is closed for this snapshot. The actual controlled Login input now triggers a navigation warning. This is evidence for that case, not blanket verification of every laboratory draft. Adjacent JSON is `independent-firstvisit-review04.json` at 11:16:35 UTC; its hardcoded sourceCandidate points to HEAD but these built assets include uncommitted work. Index SHA 79f6f9b83ffb1edc0cd1d794a3cce8e7b5f9f598e394cf7c00f4a9b3c3c8e3b4.

New receipt/assignment exercises are wired and contain no API calls. Resources are reachable again. The new quick overview suppresses page highlighting and navigation, allowing anonymous explanatory content. Preserve these improvements.

## Fix these narrow connections while completing review04

1. **Keep practice identity synthetic.** Shell lines 1373/1383 pass the live-map `selectedSampleId` into Receipt/Assignment labels, while updates still use `sampleTube` (session lines 363–372). A map selection W001 plus tube 002 must never label synthetic actions as W001. Always label practice using TRAIN-US-00 plus its own tube identity. Keep any authorized live-map reference transient and separate; clear it on actor/lab change and do not persist live entity IDs in guide session storage.
2. **Do not default a live map to a nonexistent training record.** The new map panel defaults blank selection to TRAIN-US-001 and navigates there directly. The free-text View Map button bypasses the verified permission check used by the generic navigation button. Use one shared verified-navigation path, an actually selected authorized record or verified current sample-page context, encode route segments, and give a useful selection fallback. A synthetic identifier must remain an illustration unless an explicitly isolated training environment contains that record. Ordinary application RBAC still applies; this is not evidence of backend authorization bypass.
3. **Reset resource context on lesson change.** PracticeResources uses initialResource only as a useState initializer. Equipment → Inventory reuses the component and leaves Equipment selected. Key it by chapter or synchronize the resource prop. Retain local-only behavior and translate method/resource labels, including the new hardcoded Texture/Hydrometer label.
4. **Finish all new overview copy before claiming language/science readiness.** The new `renderQuickOverviewDiagram` paragraphs are hardcoded English and use technical language such as Role-Based Access Control. Use clear localized beginner terms, with all five dictionaries and accessible labels. The new prose also asserts specific ISO/method/calibration/duplicate-QC actions and calls 485.2g a tare weight; none was established by the synthetic exercise. Use the approved illustrative sample reading/mass and explain that the lab follows its configured method and QC. Do not imply verified calibration, completed QC, custody checks, accreditation or instrument integration from an illustration. If naming a standard or specifying a ratio, verify it against the actual configured method and an authoritative source first.
5. **Keep the remaining review04 evidence tasks open.** Test the actual full/quick/role routes, completion states, immutable expiry and removal, all translated lesson states, normal sign-in/role handover and map selection. Do not convert work-in-progress or a single label assertion into full PASS. Human specialist/device/novice reviews stay pending until performed.

No core files were changed by this review. Its browser test used a frozen local build, mocked APIs and no production/database writes. Keep implementation focused in the tutorial module; continue the user's already authorized work without another approval request. Preserve the verified earlier fixes and avoid unnecessary deployment/rollback churn.

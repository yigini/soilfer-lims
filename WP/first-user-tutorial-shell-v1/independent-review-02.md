# Independent review 02 — 7ddf79b

14 September 2026, 12:12 Europe/Rome. Candidate is **not accepted for deployment**. Continue the already authorized corrections; keep the healthy existing release live. This is focused review of new changes, not a demand to restart or rewrite core LIMS.

## Independent browser evidence — 12:16 Europe/Rome

`independent-browser-review-02.cjs` ran against a snapshot of built assets TutorialShell-D67JMqvb.js / TutorialShell-nrZJIV3y.css with mocked APIs, blocked external network and no database. Results are in the adjacent JSON:

- Real UI clicks through foundations → field → Go to page issued a document request to **/projects/:projectId**, discarded `tab=connections`, and replaced the guide with Resume. No real target was highlighted.
- Verified the tube-1 preparation exercise, then selected untouched tube 2: **zero checked boxes and a disabled Verify button, but “Practice checklist verified” remained**. Store exercise verification per sample and reset/derive all feedback on identity/content changes; pH/intake/review feedback also needs this treatment. Cache feedback codes/parameters rather than already translated strings so language changes remain correct.
- At 390×844, docked **Pause, Exit and language were all invisible**, both collapsed and expanded.
- French completion still displayed **“Before you begin testing”**. App locale/override set to French without guide `lang` produced an English tutorial.
- Controlled `/api/auth/me` revalidation changed the actual app cache to **Fresh Staff**, but the dock still displayed **Old Cached Staff**. These are disposable mock identities, not production users.
- Positive: blocking the actual current TutorialShell chunk on an opted-in login left the ordinary password input present and cleanly removed the guide. This closes the earlier chunk-failure propagation defect; do not waste time undoing this correct boundary.

## Improvements to preserve

Synchronous TutorialGate now contains an error boundary outside the heavy lazy import. The beginner screen contains Tab traversal and has dialog semantics. New documents offer Resume instead of automatically reopening the full screen. Practice state is keyed by tube. These are real improvements. Source/app route guards remain unchanged and practice controls have no live mutation calls.

## Remaining release blockers

1. **No actual anchors.** `content/chapters.js` has zero `targetAnchor` properties. `TutorialShell.jsx:170–183` therefore always renders missing-target fallback. Add the reviewed inert anchors and per-lesson registry with exactly-one matching, visibility checks, bounded loading retries and route-aware cleanup. `querySelector` alone silently chooses a duplicate; no valid-target test currently exists.
2. **Page navigation is broken and can abandon work.** `TutorialShell.jsx:247–252` parses route display strings and calls `window.location.assign`. Field opens the literal `/projects/:projectId`; trace opens `/samples/:id/map`; all meaningful query parameters are discarded. Every link becomes a new unflagged document, reducing the tutorial to a Resume chip. Replace display-string parsing with typed route resolvers using authorized selected entities, existing SPA navigation and draft protection. Do not hardcode production IDs or infer a permitted record. No record/permission means an explanatory fallback. Do not take a user away from an unsaved form unconditionally.
3. **Identity is cached, not verified.** `TutorialShell.jsx:66–77` reads localStorage.user once via `useMemo([])`. Normal login, current auth revalidation, logout, role/lab changes and runtime password-change requirements cannot update that snapshot. Use existing AuthContext plus the approved module-local `/api/auth/me` verification after opt-in, with verified/anonymous/verifying/unavailable states and cancellation of stale requests. Do not change auth/backend/RBAC implementation. Clear tutorial live entity references on identity changes; retain harmless lesson progress.
4. **SPA opt-in and disable remain incomplete.** Gate only listens to mount/popstate/hashchange/custom events (`TutorialGate.jsx:36–86`). React Router Link/navigate push or replace does not emit those events. Observe router location explicitly; distinguish same-document continuation from an unflagged new document. An open session never expires: there is no active timer/visibility check, and interaction refreshes its timestamp (`useTutorialSession.js:112`). Validate expiry before Resume and before state updates.
5. **Mobile loses exit controls.** Existing tutorial media rule hides `.coach-top` and `.coach-body` at <=600px; expanding only restores the body. Docked Pause/Exit/language are in `.coach-top`. Keep these controls visibly reachable in the mobile footer/header at all times. Verify docked scenes, not only the beginner modal and viewport overflow. Do not focus a hidden title.
6. **Language coverage remains incomplete.** Initial locale reads obsolete localStorage.language, whereas LanguageContext resolves session `soilfer_locale_override` and localStorage.locale. Read the existing context, preserving a guide-specific choice. Gate Resume is English; docked strings include `Role track`, `Before you begin testing`, and the successful-target notice. Test full visible/a11y content, including recovery and mobile, not a handful of initial labels. Scientific/human sign-off must remain pending.
7. **Curriculum and mode boundaries are unfinished.** Full path still contains the 12 preview chapters; the approved assignment/readiness lesson, distinct equipment/inventory before preparation, spectral-library orientation and report/history destinations are missing. A no-account illustrated overview should remain understandable without private pages. Real-page steps require honest authorization/handover. Do not silently reduce the approved story to generic fallback text.

## Keep the acceptance contract honest

The test labels still do not match `acceptance-checklist.md`. The script now calls F01–F03 `T03`, while actual T03 covers auth wait/login/stale user/401/password changes. Cached token/user injection at lines 319–362 does not prove normal login or verified identity. The forced-password seed at 370–400 proves only an initial value, not a runtime update. Missing-target fallback alone does not prove any working anchor. Namespace presence is not version/legacy migration. Chunk existence at 955–960 is not T21 deployed build verification, and an import/mount at 963–966 is not T22 removal rehearsal.

Use the original scenario IDs and attach assertions/evidence to each. Mark partial/not run where appropriate. Run a real normal sign-in/handover in disposable fixtures, both found and missing/duplicate anchors, a resolved field/map destination, preserved query/draft, mobile exit and language, active expiry, actual feature-disable/removal and exact live asset verification. Do not relabel missing work PASS to finish the matrix.

Independent browser reproductions will be recorded separately in `independent-browser-review-02.cjs` and JSON. No production data writes or competing application edits are authorized from this review. Retain prior source improvements, repair the narrow connections, then continue CI/push/deployment without a new approval cycle once the blockers are actually resolved.

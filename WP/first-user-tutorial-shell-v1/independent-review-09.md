# Checkpoint 09 — resume after execution error; finish map verification

14 September 2026, approximately 14:48 Europe/Rome. HEAD remains 2a07e37; current changes are uncommitted. Antigravity UI reports agent execution terminated after its local client build, error 784e54b2-b155-4c02-aa27-e6715732e9e5-44331. Resume the same task and preserve current edits. No deployment task is running in the observed UI; last independently verified live release remains healthy 2a07e37 at checkpoint08.

## Accepted progress

- Absent VITE_TUTORIAL_ENABLED now evaluates false; explicit true/false behave correctly. Explicit release enablement in client/.env.production and the builder-stage Dockerfile is a small authorized configuration touch, not a change to laboratory behavior. Keep it documented in removal instructions.
- The translator now searches English root/common dictionary keys when the chosen locale is missing them. independent-source-review09.cjs/.json executes the actual extracted source function with isolated synthetic dictionaries and verifies present French, missing French common/root keys and the fallback callback. This is function evidence, not a rendered UI or production claim.
- New map lookup uses the existing authorized read-only sample detail endpoint, with appropriate pending/not-found/forbidden notices. API response shape is correct. Keep all server/RBAC code unchanged.
- T22 disposable removal already independently passed on committed 2a07e37; see independent-removal-review08.md. Do not repeat that build. The current small module-only edits and explicit build enablement do not require another broad audit.

## Complete the existing map requirement in one focused pass

1. **Invalidate asynchronous lookup on context changes and guide teardown.** In TutorialShell.jsx, sampleCheckSeqRef increments only when another request starts (around527). The actor/lab clearing effect around86–96 clears references but does not invalidate or cancel the pending request. A delayed response still uses the old captured actor and can navigate after actor/lab/token change, chapter/sample change, or Exit/unmount. Use a bounded abortable request lifecycle, invalidate on these changes, and check the current verified actor before routing. Cancelling must be silent; timeouts/malformed success responses should leave a helpful retry notice instead of an endless verifying message. Preserve current dirty-form navigation protection. No global navigation or auth rewrite.
2. **Route using the canonical ID returned by the authorized lookup.** /api/samples/:id/detail resolves id, labId or originalId (sampleController around1001–1018). The map-state endpoint resolves internal ID (around1222–1227). The current success branch verifies a human sample label and then routes with that same input, which can produce a map-state404. Use res.data.sample.id in the verified cache and map URL, with single encoding; retain the human input for display. Do not force users to know a UUID.
3. **Prove the intended cases with synthetic fixtures.** Authorized human-readable alias -> canonical map; signed-in wrong-lab record -> no navigation; delayed request -> actor/lab change or Exit -> no stale navigation; same-document account/lab change while the old URL remains -> new access verification. Source review of the current T23 still sees a storage-only logout assertion and unused forbidden fixture token; finish the real behavior test. A frozen local-browser delayed-response/Exit check is underway independently; accept its bounded evidence if available in independent-map-race-review09 files.

## Keep temporary code and claims clean

- Remove shipping window.__SOILFER_TUTORIAL_LOCALES__ and window.__SOILFER_TUTORIAL_T__ testing hooks. They are assigned globally with no cleanup and remain after Exit. The production tutorial does not need them. Test the translator through an isolated function/fixture or a disposable test-only build, and verify rendered fallback in that context. Do not add a new public test API.
- Current T11 has improved to actual role selection but asserts the first stop and counters, not complete traversal of each role path. Label what is actually tested, preserving existing separate auth evidence.
- T14's narrower claims are an improvement; retain specific automated coverage and pending human scientific/novice/device review. Current T21 still hardcodes b7fd307; record actual candidate/assets per release.

Do not restart completed work, revert the whole application, change the model or request another approval. Continue safe focused implementation, relevant checks, GitHub push/CI and deployment under existing authorization. Give the user short plain-English updates: what is fixed, what is being checked, what remains. An execution error is not proof of a code defect or a reason to change laboratory data.

## Independent browser evidence — delayed response after Exit reproduced

At 12:48:36 UTC, independent-map-race-review09.cjs/.json tested frozen local TutorialShell-BQdbTeft.js (SHA ee2c15033e412d5887983d9894e880d20c66e9e110421f026489bee9ed4633f3), index SHA 84d29eef7b8b5fad622ed9908bcee82676fc9be168ea343b61cb8a34db2fbc90. With synthetic technician authentication, the sample detail GET was held pending. Exit returned to /workbench, guide count0, session null. Releasing the delayed200 then navigated to /samples/TEST-SAMPLE/map?tutorialmode=true and recreated tutorial storage. The guide DOM was still absent at the final observation, so the proven defect is unwanted navigation and session restoration after Exit, not a claim of visible reopening. All APIs mocked, external requests blocked, zero browser errors or attempted writes; no backend/database/production access. Reuse this narrow reproducer after the lifecycle fix instead of rerunning unrelated suites.

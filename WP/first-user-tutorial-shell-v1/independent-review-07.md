# Checkpoint 07 — narrow interim feedback while review06 is in progress

14 September 2026, approximately 14:03 Europe/Rome. HEAD remains b7fd307; current tutorial changes are uncommitted and not a new production release. No competing application or database edits.

## Confirmed progress

Independent local browser check `independent-lifecycle-review07.cjs/.json` against TutorialShell-DIYaXvkB.js / TutorialShell-D1vt1_Ss.css confirms the French path-choice and login illustration fixes. Next from the chooser records `viewed: [0]`, `practiced: []`, and quick progress reads 02/09. Source separates successful practice callbacks from Next, migrates old ambiguous progress conservatively, adds texture to the quick path and fixes double URI encoding/malformed decoding. Preserve these improvements. Snapshot index SHA d733898cdba8bfb0a9cc084d51087afda35194c7e414adae4ae54d7e148639b9. All APIs mocked, external requests blocked, no mutations or page errors.

## Three specific follow-through points

1. **Availability must react while a guide is open.** Current `tutorialConfig.js` is enabled by default and reads a runtime/window/session toggle, but Gate only reevaluates on a route/render. In the actual built app, setting both supported disabled flags and dispatching document/window visibilitychange leaves the guide visible and guide storage intact in the same document. The current T17 uses `page.goto('/login')` after the toggle, so it tests a new document instead of retirement. Add the agreed default-off build availability setting and a bounded opt-in-only recheck/event/expiry mechanism that deactivates an already-open guide. Test without navigation/reload. Preserve no ordinary-page timer/listener/heavy-load behavior. This is a module availability control, not access control. Do not invent a production retirement date.
2. **Optional chapters cannot produce impossible progress.** Quick → open Curriculum → Trace index13 displays **14/09**, with aria-valuenow14 above max9. Keep optional chapter browsing useful but label it outside the current track (and retain honest track progress), or constrain jumps appropriately. The local browser check reproduces it. Update `.chapter.practiced`/`.chapter.viewed` styling if retaining the existing completion treatment; current CSS still styles `.chapter.done`.
3. **T22 still has no removed build.** Replacing the mount/import and calling `esbuild.transformSync` only parses one JSX string. The following login/Help/workbench checks still serve the original build. Rehearse in a disposable client copy with the mount/module removed, build that copy, then serve/check it. Keep the real checkout untouched. Do not label the transform as a completed removal rehearsal, or silently PASS if esbuild is missing. Run once on the final candidate rather than repeatedly while editing.

The conditional translation-fallback requirement is also worth completing accurately: the newly added footer notice is unconditional, even when no fallback occurred. Track actual missing/fallback keys and show the notice only when applicable; localize the remaining foundation/glossary labels as already requested in review06.

The authorized map lookup/loading/not-found/forbidden work remains in progress; source still derives an ID from text/path/query. No additional map prompt is needed beyond finishing review06. No new broad audit, core code changes or approval cycle is requested. Continue focused implementation and honest technical evidence. Latest independently verified live release remains b7fd307 at 13:44; do not redeploy until the focused candidate is ready, and do not roll back the whole application for tutorial lesson gaps.
